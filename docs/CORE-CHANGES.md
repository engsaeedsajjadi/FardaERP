# FardaERP — Core Changes Ledger

> **Rule:** every modification to an upstream ERPNext/Frappe file in this
> repository MUST be recorded here: file, change, reason, risk, regression test.
> "No entry, no change." See docs/VERSION-BASELINE-AND-ARCHITECTURE.md §5.3.

| # | Date | File(s) | Change | Reason | Risk | Regression test |
|---|---|---|---|---|---|---|
| CORE-001 | 2026-09-15 | `erpnext/modules.txt` | Appended module `Farda Iran` (→ `erpnext/farda_iran/`) after upstream entry `Subcontracting`/`EDI` | Approved Single-Repository architecture: isolated module for all Iranian localization (docs/VERSION-BASELINE-AND-ARCHITECTURE.md §5) | Low — modules.txt is the sanctioned extension point; Frappe creates the Module Def on install | Site install succeeds; `frappe.get_modules()` includes `farda_iran`; upstream test suite unaffected |
| CORE-002 | 2026-09-15 | `.github/workflows/*` (upstream Frappe/ERPNext CI) | Removed all upstream workflow files; FardaERP will add its own pipelines in the CI/CD phase | (1) GitHub App used by this environment cannot push workflow modifications (no `workflows` permission); (2) upstream release/translation automation must never run on the FardaERP fork | Low — CI-only files, no runtime code; upstream restore possible from tag `v16.34.2` at any sync | G-gates + future CI run green |
| CORE-003 | 2026-09-15 | `erpnext/hooks.py` (appended section only) | Added `before_migrate` hook + `doc_events.update({Customer/Supplier/Company/Sales Invoice/Purchase Invoice → farda_iran validators & VAT service})` at end of file | Wiring the isolated `farda_iran` localization into the framework — hooks are the sanctioned extension point; no upstream handler modified (dict `.update()` merge only) | Low — validators are no-ops when custom fields are empty; VAT applies only when `farda_apply_vat` is checked | `tests/test_integration_iran.run` (5 PASS incl. negative validation) + Gate-5 smoke 13/13 regression-free |

## Environment-level patches (outside this repo, /opt/fardabench)

| # | File (upstream install tree) | Change | Reason |
|---|---|---|---|
| ENV-1 | apps/hrms/.../patches/post_install/update_employee_advance_status.py | `(advance.return_amount)` → `(advance.return_amount > 0)`, `(advance.claimed_amount & advance.return_amount)` → `(advance.claimed_amount > 0) & (advance.return_amount > 0)` | bare numeric column as boolean is MySQL-only; PostgreSQL raises "argument of AND must be type boolean" during hrms after_install. Must be re-applied if the hrms tree is re-synced (finish_env.sh step 5 does it). |
| ENV-2 | PG cluster init | re-initdb `--encoding=UTF8 --no-locale` | pgserver wheel initialized SQL_ASCII; non-ASCII DocType data crashed inserts with UnicodeEncodeError. Persian-first ERP requires UTF8 storage. |

## Test-infrastructure fixes in this repo

| Commit | File | Bug |
|---|---|---|
| f0c810f | farda_iran/tests/test_integration_iran.py | `frappe.db.savepoint()` returns None; using its value as `save_point=` made `frappe.db.rollback(None)` run a FULL rollback, silently wiping uncommitted VAT settings mid-test. Now uses frappe's `savepoint()` context-manager with `catch=()`. |

## hooks.py (upstream file) — Farda additions

| Date | Section | Addition | Reason |
|---|---|---|---|
| 2026-09-15 | `jinja.methods` | 6 methods: `farda_iran.invoice.persian.{fa, format_jalali_date, money_words_irr, money_words_toman, toman_str}` + `farda_iran.tax.service.invoice_totals` | Persian print formats (Farda Persian Invoice) need Jalali/Toman/Persian-digits/amount-in-words/VAT summary inside server-side Jinja rendering. |
| 2026-09-15 | `app_include_css` | `"erpnext.bundle.css"` → list + `/assets/erpnext/farda_iran/css/farda_rtl.css` | H8 RTL layer for fa-locale Desk; CSS scoped to `html[lang="fa"]` only. |
| CORE-004 | 2026-09-16 | `.gitignore` (appended `.env` + comment at end of file) | §26 Docker stack: `docker-compose.yml` reads secrets from `.env` (copy of `.env.example`); upstream `.gitignore` lacked the entry, and committing real credentials is prohibited | Prevent accidental commit of production secrets; zero runtime effect | None — VCS-only change; upstream sync may re-add file harmlessly (re-append after sync gate) | `git check-ignore .env` |
| CORE-005 | 2026-09-16 | `erpnext/hooks.py` (doc_events.update dict extended) | §24 audit trail: bound `farda_iran.audit.service.on_doc_update` to Customer/Supplier/Bank Account (after_insert+on_update) and Farda Cheque / Farda VAT Settings (same events); Payment Entry on_submit/on_cancel handlers converted string→list appending `farda_iran.audit.service.on_payment_entry` (existing cheque payment_link handlers untouched, first in list) | Auditable trail requirement (payment/cheque/VAT-change/identity-data; old/new, user, ts, IP; no secrets/OTP plaintext) | Low — pure additions via dict .update(); existing handlers unchanged and still run first | R14 audit E2E (test_audit_runtime.py) + full regression |
| CORE-006 | 2026-09-16 | `erpnext/hooks.py` (comment-only addition documenting the health endpoint) | § Monitoring: `erpnext.farda_iran.monitoring.api.health` is a guest-readable, rate-limited health endpoint for LB/uptime probes; a hook-level comment marks it as a monitored contract (the endpoint itself is a pure addition inside farda_iran; whitelist registration is decorator-driven, no hook mutation needed) | Load balancers/uptime monitors need an unauthenticated health probe; § Monitoring requires it never expose secrets/PII | None — comment only | R17 test_monitoring_runtime.py (contract + dependency-fail probes) |
| CORE-012 | 2026-09-20 | `docker/Dockerfile` (full rewrite), `docker-compose.yml` (worker/scheduler healthchecks + frontend header), `docker/nginx/default.conf.template` | **User-authored stabilization commit `4a3e763`** ("stabilize FardaERP v16 production stack"): Dockerfile restructured around `bench build` with pre-seeded `sites/apps.txt` + minimal `common_site_config.json` (keeps CORE-011 semantics) and yarn via `corepack enable + prepare yarn@1.22.22` after dangling-shim rm (corepack variant of CORE-010, pre-validated CACHED on the user machine); healthchecks `pgrep -f "bench worker|schedule"` -> `"frappe worker|schedule"` (matches real process titles in v16 images); frontend switched from Host-header routing to fixed `FRAPPE_SITE_NAME_HEADER: erp.farda.local` + nginx `server_name _` (single-site default; override via docker-compose.override.yml) | Third/fourth real-run findings on the user machine; the same commit removed the CORE-010/011 ledger rows + readiness addenda (2)/(3), which were RESTORED by this docs commit to preserve history-vs-current separation | Medium - build/runtime packaging; asset output contract unchanged (sites/assets -> static-assets copy incl. per-app public/); site-naming contract changed (see DOCKER.md fixed-site note) | user rerun on 4a3e763 + RTL batch3 tip pending |
| CORE-011 | 2026-09-17 | `docker/Dockerfile` (§26 artifact, builder assets step) | **CONFIG-BUG #3 (two-part) surfaced by the user's third real Docker run** (builder step 13): (a) erpnext's `banking/` vite app reads `sites/common_site_config.json` (`proxyOptions.ts` → `webserver_port`) during `yarn build`; the file does not exist in the builder → vite `ENOENT` killed the build. (b) install+build were interleaved per app while bench/esbuild resolves imports across app trees (frappe's build bundles erpnext/hrms entry points) → unresolved imports (`onscan.js`, `html2canvas`) during frappe's build. Fix: create the minimal common_site_config (`{"webserver_port": 8000}`) before builds; split into install-all-then-build-all. Both invisible in-sandbox (wheel-only build stage) — real-run findings again | Low — build-stage only; same assets output (sites/assets) | user rerun pending |
| CORE-010 | 2026-09-17 | `docker/Dockerfile` (§26 artifact, builder stage) | **CONFIG-BUG #2 surfaced by the user's second real Docker run** (builder step 12/13 died in `corepack enable`: `ENOENT realpath /usr/local/bin/yarn`): node:24 bundles yarn 1.x at `/opt/yarn-v1.22.x` with only a **symlink** in `/usr/local/bin`; the selective `COPY --from=nodebin /usr/local/bin` leaves that symlink dangling, breaking corepack and direct `yarn`. Fix: drop dead yarn/yarnpkg/pnpm shims and `npm install -g yarn@1.22.22` (npm survives the COPY as real files). Local proof: npm-installed yarn 1.22.22 runs; layout `yarn -> /opt/yarn-v1.22.22/bin/yarn` confirmed in a same-layout env. NOTE: the user's local Dockerfile (corepack variant) matches NO pushed commit (`git log -S corepack` empty branch-wide) — their checkout is stale; fresh clone required | Image build never executed in sandbox (BLOCKED-ENV) — packaging bugs surface only on real runs | Low — build-stage only; runtime image unaffected | user rerun pending (fresh clone + docker compose build) |
| CORE-009 | 2026-09-17 | `.dockerignore` (upstream file, modified at §26 — that modification was NOT recorded then; this entry covers both) + `docker-compose.yml` (§26 artifact) | **CONFIG-BUG found by the first real Docker run (user machine, Windows):** `.dockerignore` excluded `*.md`, stripping `README.md` from the build context while `pyproject.toml` declares `readme = "README.md"` → `flit_core` ConfigError killed `pip install -e apps/erpnext` at builder step 9/12. Fix: `!README.md` exception after `*.md` (`!LICENSE` kept). Also: `FRAPPE_SITE_NAME_HEADER: ${...:-$$host}` triggered cross-version compose interpolation warnings (`The "host" variable is not set` ×4); replaced with the frappe_docker-proven hard default `$$host` (override via docker-compose.override.yml) | Image build was BLOCKED-ENV in sandbox so the packaging bug could only surface on a real Docker run — exactly the Gate-1 gap the verification report predicted | Low — packaging metadata only; no runtime code | Local context simulation: pruned-context `pip wheel` (flit_core) reproduces the exact ConfigError with the old rules and passes metadata prep with the fix (host-py version check after it is expected, image is py3.14); user rerun pending |
| CORE-008 | 2026-09-16 | `README.md` (upstream ERPNext landing page) | Replaced wholesale with the FardaERP product README (Persian-first: product identity, feature layers, honest verification status incl. 5 BLOCKED-ENV gates + RTL/Reports PARTIAL, quick start, docs index, GPL-3.0 + non-affiliation block) | §47 verification feedback: the repo landing page still presented vanilla ERPNext (frappe.io links, upstream badges, frappe_docker instructions) — product presentation contradicted the actual codebase; reviewer flagged it as the top documentation gap | Low — docs-only, no code; upstream content recoverable from tag `v16.34.2` at any sync | None (docs-only); pipeline build stage unaffected (wheel/JS checks do not read README) |
| CORE-007 | 2026-09-16 | `erpnext/hooks.py` (`scheduler_events.daily` string → list, appended `farda_iran.notifications.service.run`) | §33 notification triggers: daily scheduler entry for the 6-trigger notification service (cheque reminder entry preserved first) | In-app Notification Log + SMS channel fan-out; dedupe ever per (doctype, name, user); SMS recipients env-only (`FARDA_ALERT_SMS_NUMBERS`) | Low — pure list extension; existing cheque reminder path unchanged | R20 test_notifications_runtime.py (6/6) + R18 perf suite (reminders) |
